apply plugin: 'java'
apply plugin: 'application'

mainClassName = "sal162.EX3"

repositories {
   mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

run {    
    systemProperties System.getProperties()

    args System.getProperty("args").split()    
}

task sequencehelp << {
println "Integer Sequence Project"
println "Enter first argument either 'lazy' or 'triangle' and second argument is a postivie integer or zero. \nProgram will print out the result in the format 'Tri(n) = x' if the user selected a triangle number or 'Lazy(n) = x' if user selected lazy" 

}